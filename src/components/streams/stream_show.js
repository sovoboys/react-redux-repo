import React from 'react';

class StreamShow extends React.Component {
    
    render () {
        return (
            <div className="stream-show">StreamShow</div>
        );
    };
};

export default StreamShow;