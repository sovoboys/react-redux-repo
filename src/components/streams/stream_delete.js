import React from 'react';

class StreamDelete extends React.Component {
    
    render () {
        return (
            <div className="stream-delete">StreamDelete</div>
        );
    };
};

export default StreamDelete;